#include "syscall.h"
#include "copyright.h"
#define maxlen 32
int main()
{
	//int len;
	//char filename[maxlen +1];
/*Create a file*/
	/*int x;
	x=10;
	if (Create("texTTTT.txt") == -1)
	{
	// xuất thông báo lỗi tạo tập tin
	//printf("xuất thông báo lỗi tạo tập tin");

		PrintInt(x);
	}
	else
	{
	// xuất thông báo tạo tập tin thành công
	//printf("xuất thông báo tạo tập tin thành công");
		PrintInt(x);
	}*/
	PrintChar(a);
	Halt();
}
